## Open terminal at root directory and run below command to compile all files:
```bash
javac app/*.java command/*.java model/*.java service/*.java strategy/*.java factory/*.java exception/*.java
```

## Run the application with sample test cases:
```bash
java app.Main < sample.txt
```
